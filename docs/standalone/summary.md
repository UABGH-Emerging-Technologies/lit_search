# Standalone Summary

A one-step endpoint that searches PubMed for a research question, summarizes
the retrieved article abstracts, and returns a formatted DOCX document with
the summary and APA citations.

**Input:** Research question + LLM credentials

**Output:** Literature summary (base64-encoded DOCX)

## Workflow

::: ScopingReview.Standalone.Workflow
